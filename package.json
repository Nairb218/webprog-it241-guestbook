{
    "name": "guestbook-app",
    "version": "1.0.0",
    "private": true,
    "description": "Full-stack Guestbook app — React + NestJS + Supabase",
    "scripts": {
        "dev:client": "cd client && npm run dev",
        "dev:server": "cd server && npm run start:dev",
        "install:all": "cd client && npm install && cd ../server && npm install"
    }
}